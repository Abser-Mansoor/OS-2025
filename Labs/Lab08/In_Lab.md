## Part(a) : parameter passing allows runner to dynamically receive values at runtime, making it reusable for different computations.
## Part(b) : Attributes learned in the current lab are the defaults only
